udp:
  routers:
    wg-router:
      entryPoints:
        - wireguard
      service: wg-service

  services:
    wg-service:
      loadBalancer:
        servers:
          - address: "edulution-wireguard:443"
